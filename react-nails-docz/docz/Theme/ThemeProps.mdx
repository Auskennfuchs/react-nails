---
name: Theme-Properties
menu: Theme
---

import { Playground } from "docz";
import { ThemeProps } from "react-nails";

# ThemeProps

The full theme object is listed here. Every property can be overriden by a custom theme.

<ThemeProps>
    {theme => <code>{JSON.stringify(theme, null, 2, true)}</code>}
</ThemeProps>
