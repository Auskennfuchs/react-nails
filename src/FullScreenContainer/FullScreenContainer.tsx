import styled from 'styled-components'
import { Column } from '../layout'

const FullScreenContainer = styled(Column)`
    width: 100%;
    height: 100vh;
    position: relative;
`

export default FullScreenContainer